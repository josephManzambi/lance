# Getting started

> Pending v0.1.

Will cover:

1. Install AART with `uv sync`.
2. Install and configure Ollama with a judge model.
3. Stand up a deliberately vulnerable MCP server (examples provided).
4. Write an `authorized_targets.yaml` including your local test hosts.
5. Run `aart run --target ... --attack indirect-injection-via-tool-output`.
6. Read the resulting Finding JSON and Markdown report.
