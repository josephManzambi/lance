# Hosts LANCE is authorized to target. Anything not in this list triggers
# TargetAuthorizationError. Copy to authorized_targets.yaml (gitignored).

authorized:
  - localhost
  - 127.0.0.1
  - "*.local"
  # Add explicit hosts you have authorization to test:
  # - staging.example.com
